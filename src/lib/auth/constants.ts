/** Cookie holding a per-browser guest id used in keyless demo mode. */
export const GUEST_COOKIE = "after_guest";

/** How long a guest identity persists. */
export const GUEST_COOKIE_MAX_AGE = 60 * 60 * 24 * 180; // 180 days

export const GUEST_ID_PREFIX = "guest_";
